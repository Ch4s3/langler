[package]
name = "classifier_nif"
version = "0.1.0"
edition = "2021"

[lib]
name = "classifier_nif"
crate-type = ["cdylib", "staticlib"]

[dependencies]
rustler = "0.37"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
regex = "1.10"

[profile.release]
opt-level = 3
